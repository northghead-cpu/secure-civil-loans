import { useEffect, useMemo, useState } from "react";
import { useNavigate } from "react-router-dom";
import { Helmet } from "react-helmet-async";
import { motion } from "framer-motion";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import LenderHandoffModal, { type HandoffOffer } from "@/components/LenderHandoffModal";
import { LampContainer } from "@/components/ui/lamp";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Slider } from "@/components/ui/slider";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { ArrowRight, CheckCircle2, Info, Loader2 } from "lucide-react";
import { useAuth } from "@/hooks/useAuth";
import { useRBAC } from "@/hooks/useRBAC";
import { supabase } from "@/integrations/supabase/client";

type ProductType = "loan_bank" | "loan_microfinance" | "salary_advance_microfinance";
interface LoanOffer extends HandoffOffer { processingTime?: string; productType: ProductType; }
interface BankProduct { id: string; bank_name: string | null; product_type: ProductType; interest_rate: number | null; min_amount: number | null; max_amount: number | null; max_term_months: number | null; processing_days: number | null; }
const PRODUCT_TYPE_LABELS: Record<ProductType, string> = { loan_bank: "Bank", loan_microfinance: "Microfinance", salary_advance_microfinance: "Salary Advance — Microfinance" };
const calculateMonthlyPayment = (principal: number, annualRate: number, months: number) => { if (months <= 0) return 0; const monthlyRate = annualRate / 100 / 12; if (monthlyRate === 0) return principal / months; return principal * (monthlyRate * Math.pow(1 + monthlyRate, months)) / (Math.pow(1 + monthlyRate, months) - 1); };
const ComparePage = () => {
  const [amount, setAmount] = useState([100000]); const [sortBy, setSortBy] = useState("rate"); const [termFilter, setTermFilter] = useState("all"); const [providerFilter, setProviderFilter] = useState("all"); const [selectedOffer, setSelectedOffer] = useState<LoanOffer | null>(null); const [products, setProducts] = useState<BankProduct[]>([]); const [productsLoading, setProductsLoading] = useState(true); const [productsError, setProductsError] = useState<string | null>(null); const navigate = useNavigate(); const { user, profile, loading, profileLoading, refreshProfile } = useAuth(); const { hasRole } = useRBAC();
  useEffect(() => { if (user) refreshProfile(); }, [user, refreshProfile]);
  useEffect(() => { if (loading || profileLoading) return; if (!user) { navigate("/login", { replace: true }); return; } const isAdmin = hasRole("super_admin") || hasRole("admin") || hasRole("super_user"); if (!isAdmin && profile?.kyc_status !== "VERIFIED" && profile?.kyc_status !== "COMPLETED") navigate("/apply", { replace: true }); }, [user, profile, loading, profileLoading, hasRole, navigate]);
  useEffect(() => { if (!user) return; let cancelled = false; const loadProducts = async () => { setProductsLoading(true); setProductsError(null); const query = (supabase.from("bank_products") as any).select("id, bank_name, product_type, interest_rate, min_amount, max_amount, max_term_months, processing_days").eq("active", true).order("interest_rate", { ascending: true }); const { data, error } = await query; if (cancelled) return; if (error) { setProductsError("We couldn't load current lender offers. Please try again."); setProducts([]); } else setProducts((data ?? []) as BankProduct[]); setProductsLoading(false); }; loadProducts(); return () => { cancelled = true; }; }, [user]);
  const offers = useMemo<LoanOffer[]>(() => products.filter((p) => { const isSalaryAdvance = p.product_type === "salary_advance_microfinance"; const sectionMatches = isSalaryAdvance ? false : providerFilter === "all" || (providerFilter === "bank" && p.product_type === "loan_bank") || (providerFilter === "microfinance" && p.product_type === "loan_microfinance"); const min = p.min_amount ?? 0; const max = p.max_amount ?? Number.MAX_SAFE_INTEGER; const maxTerm = p.max_term_months ?? 0; const requestedTerm = termFilter === "all" ? maxTerm : Number(termFilter); return sectionMatches && amount[0] >= min && amount[0] <= max && maxTerm >= requestedTerm; }).map((p) => { const term = termFilter === "all" ? (p.max_term_months ?? 0) : Number(termFilter); const rate = p.interest_rate ?? 0; const monthlyPayment = calculateMonthlyPayment(amount[0], rate, term); return { id: p.id, lender: p.bank_name || "Participating financial institution", rate, maxAmount: p.max_amount ?? 0, term, monthlyPayment, totalCost: monthlyPayment * term, processingTime: p.processing_days != null ? `${p.processing_days} business day${p.processing_days === 1 ? "" : "s"}` : undefined, productType: p.product_type }; }), [products, amount, termFilter, providerFilter]);
  const salaryAdvanceOffers = useMemo<LoanOffer[]>(() => products.filter((p) => { const min = p.min_amount ?? 0; const max = p.max_amount ?? Number.MAX_SAFE_INTEGER; const maxTerm = p.max_term_months ?? 0; const requestedTerm = termFilter === "all" ? maxTerm : Number(termFilter); return p.product_type === "salary_advance_microfinance" && amount[0] >= min && amount[0] <= max && maxTerm >= requestedTerm; }).map((p) => { const term = termFilter === "all" ? (p.max_term_months ?? 0) : Number(termFilter); const rate = p.interest_rate ?? 0; const monthlyPayment = calculateMonthlyPayment(amount[0], rate, term); return { id: p.id, lender: p.bank_name || "Participating microfinance institution", rate, maxAmount: p.max_amount ?? 0, term, monthlyPayment, totalCost: monthlyPayment * term, processingTime: p.processing_days != null ? `${p.processing_days} business day${p.processing_days === 1 ? "" : "s"}` : undefined, productType: p.product_type }; }), [products, amount, termFilter]);
  const sortedOffers = (items: LoanOffer[]) => [...items].sort((a, b) => sortBy === "rate" ? a.rate - b.rate : sortBy === "monthly" ? a.monthlyPayment - b.monthlyPayment : a.totalCost - b.totalCost);
  const renderOffers = (items: LoanOffer[], emptyTitle: string, emptyDescription: string) => { const filtered = sortedOffers(items); return filtered.length === 0 ? <motion.div className="rounded-2xl border border-border/60 bg-card p-8 sm:p-12 text-center card-elevated"><div className="mx-auto mb-5 flex h-12 w-12 items-center justify-center rounded-full bg-primary/10"><Info className="h-6 w-6 text-primary" /></div><Badge variant="outline" className="mb-4">Verified offers only</Badge><h2 className="text-2xl font-display font-semibold text-foreground">{emptyTitle}</h2><p className="mx-auto mt-3 max-w-xl text-muted-foreground leading-relaxed">{emptyDescription}</p><div className="mx-auto mt-5 inline-flex items-center gap-2 rounded-full border border-border/60 bg-background px-3 py-1.5 text-sm text-muted-foreground"><CheckCircle2 className="h-4 w-4 text-primary" />Your profile is verified</div></motion.div> : <div className="space-y-4">{filtered.map((offer) => <motion.div key={offer.id} className="bg-card rounded-xl p-6 border border-border/50 card-elevated"><div className="flex flex-col lg:flex-row lg:items-center gap-6"><div className="flex-1"><div className="flex flex-wrap items-center gap-2 mb-2"><h2 className="font-display font-semibold text-foreground text-base">{offer.lender}</h2><Badge variant="outline">{PRODUCT_TYPE_LABELS[offer.productType]}</Badge></div><div className="flex flex-wrap items-center gap-x-3 gap-y-1 text-xs text-muted-foreground">{offer.processingTime && <span>Processing: {offer.processingTime}</span>}<span>Maximum: K{offer.maxAmount.toLocaleString()}</span></div></div><div className="grid grid-cols-3 gap-6 lg:gap-10 text-center"><div><p className="text-2xl font-display font-bold text-foreground">{offer.rate}%</p><p className="text-xs text-muted-foreground">Annual Rate</p></div><div><p className="text-2xl font-display font-bold text-foreground">K{Math.round(offer.monthlyPayment).toLocaleString()}</p><p className="text-xs text-muted-foreground">Monthly</p></div><div><p className="text-2xl font-display font-bold text-foreground">{offer.term} mo</p><p className="text-xs text-muted-foreground">Term</p></div></div><Button onClick={() => setSelectedOffer(offer)}>View offer <ArrowRight className="w-4 h-4" /></Button></div></motion.div>)}</div>; };
  if (loading || profileLoading || !user || productsLoading) return <div className="min-h-screen bg-background flex items-center justify-center"><Loader2 className="w-8 h-8 animate-spin text-muted-foreground" /></div>;
  return <div className="min-h-screen bg-background"><Helmet><title>Compare Loan Options — Riverbanc</title><meta name="description" content="Compare verified loans and salary advances from participating financial institutions through Riverbanc." /><link rel="canonical" href="https://riverbanc.co.zm/compare" /></Helmet><Navbar /><main className="pt-24 pb-16"><LampContainer className="h-48 bg-background"><span /></LampContainer><motion.div className="container mx-auto px-4 lg:px-8 mt-6" initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }}><div className="max-w-3xl"><h1 className="text-3xl md:text-4xl font-display font-bold text-foreground mb-2">Compare Loan Options</h1><p className="text-muted-foreground text-lg">Choose the type of borrowing you need, then compare verified offers from participating financial institutions.</p></div></motion.div><div className="container mx-auto px-4 lg:px-8 mt-6"><Tabs defaultValue="loans" onValueChange={(value) => { if (value === "loans") setProviderFilter("all"); }}><TabsList className="grid w-full max-w-xl grid-cols-2 mb-6"><TabsTrigger value="loans">Loans</TabsTrigger><TabsTrigger value="salary-advance">Salary Advance</TabsTrigger></TabsList><div className="bg-card rounded-xl p-6 border border-border/50 card-elevated mb-8"><div className="grid md:grid-cols-4 gap-6 items-end"><div><label className="text-sm font-medium text-foreground mb-2 block">Amount: K{amount[0].toLocaleString()}</label><Slider value={amount} onValueChange={setAmount} min={10000} max={500000} step={5000} className="mt-3" /><div className="flex justify-between text-xs text-muted-foreground mt-1"><span>K10,000</span><span>K500,000</span></div></div><div><label className="text-sm font-medium text-foreground mb-2 block">Term</label><Select value={termFilter} onValueChange={setTermFilter}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All Terms</SelectItem><SelectItem value="36">36 Months</SelectItem><SelectItem value="48">48 Months</SelectItem><SelectItem value="60">60 Months</SelectItem><SelectItem value="72">72 Months</SelectItem></SelectContent></Select></div><div><label className="text-sm font-medium text-foreground mb-2 block">Sort By</label><Select value={sortBy} onValueChange={setSortBy}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="rate">Interest Rate</SelectItem><SelectItem value="monthly">Monthly Payment</SelectItem><SelectItem value="total">Total Cost</SelectItem></SelectContent></Select></div><TabsContent value="loans" className="m-0"><label className="text-sm font-medium text-foreground mb-2 block">Provider</label><Select value={providerFilter} onValueChange={setProviderFilter}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Banks & Microfinance</SelectItem><SelectItem value="bank">Banks only</SelectItem><SelectItem value="microfinance">Microfinance only</SelectItem></SelectContent></Select></TabsContent><TabsContent value="salary-advance" className="m-0"><div className="rounded-lg border border-border/60 bg-muted/30 px-3 py-2 text-sm text-muted-foreground">Microfinance institutions only</div></TabsContent></div></div><TabsContent value="loans" className="m-0">{productsError ? <div className="rounded-2xl border border-destructive/30 bg-destructive/5 p-8 text-center"><Info className="h-6 w-6 mx-auto mb-3 text-destructive" /><h2 className="text-xl font-display font-semibold">Offers temporarily unavailable</h2><p className="mt-2 text-sm text-muted-foreground">{productsError}</p><Button variant="outline" className="mt-5" onClick={() => window.location.reload()}>Try again</Button></div> : renderOffers(offers, "No matching loan offers yet", "No participating bank or microfinance institution currently has a verified loan product matching this amount and term.")}</TabsContent><TabsContent value="salary-advance" className="m-0">{productsError ? <div className="rounded-2xl border border-destructive/30 bg-destructive/5 p-8 text-center"><Info className="h-6 w-6 mx-auto mb-3 text-destructive" /><h2 className="text-xl font-display font-semibold">Offers temporarily unavailable</h2><p className="mt-2 text-sm text-muted-foreground">{productsError}</p></div> : renderOffers(salaryAdvanceOffers, "No salary advances available yet", "No participating microfinance institution currently has a verified salary advance matching this amount and term.")}</TabsContent></Tabs><p className="mt-8 text-center text-xs text-muted-foreground">Riverbanc is a technology platform, not a bank or lender. Participating financial institutions provide the products and make lending decisions.</p></div></main><Footer /><LenderHandoffModal open={Boolean(selectedOffer)} offer={selectedOffer} requestedAmount={amount[0]} onClose={() => setSelectedOffer(null)} /></div>;
};
export default ComparePage;
